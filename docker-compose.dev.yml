# ===========================================================================
# Trazza — override de DESARROLLO LOCAL
# Buildea las imágenes localmente en vez de tirar de GHCR.
#
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
# ===========================================================================

name: trazza

services:
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    environment:
      NODE_ENV: development

  bridge:
    build:
      context: ./apps/api
      dockerfile: Dockerfile

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
