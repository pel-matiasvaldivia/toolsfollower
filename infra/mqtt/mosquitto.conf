# Mosquitto — ingesta de dispositivos GPS/LoRa (MVP)
listener 1883
# MVP: acceso anónimo. En producción, crear password_file / ACLs por device.
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
